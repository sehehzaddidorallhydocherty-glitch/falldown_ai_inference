package com.example.re

object AppConfig {
    const val USE_HTTP_SERVICE = true
    // Acts as the default address and fallback port for the runtime-configurable server field.
    const val API_BASE_URL = "http://192.168.4.181:8080"
}
